<div class="">
  <h1><%= @cocktail.name %></h1>
</div>

<% if  @doses.count == 0   %>

<div class="d-none white">
  <% end %>
  <h3 >Ingredients</h3>
  <br>
  <table class="table table-striped bg-light rounded" >
    <thead>
      <tr>
        <th scope="col">Dose</th>
        <th scope="col">Ingredient</th>

      </tr>
    </thead>
    <% @doses.each do |dose| %>
    <tbody>
      <tr>
        <td class="w-80"><%= dose.description %></td>
        <td class=''><%= dose.ingredient.name %><%= link_to "🗑", cocktail_dose_path(params[:id], dose),
    method: :delete,
    data: { confirm: "Are you sure you want to delete this task?" } %>

</td>
      </tr>
    </tbody>
    <% end %>
  </table>
</div>
  <div class="text-right container">
    <%= link_to 'add a new ingredient / Dose', new_cocktail_dose_path(@cocktail) , class: 'btn btn-primary '%>
  </div>



